---
# ── NON-PROJECT GRANT TEMPLATE ──────────────────────────────────
# Use this for grants that fund the ORGANISATION (insurance, software,
# running costs) rather than a specific public project.
#
# HOW TO ADD A GRANT:
#   1. Make a copy of this file in the same folder (src/content/grants/).
#   2. Rename it, e.g. "swan-volunteer-grant.md" (lowercase, dashes, no spaces).
#   3. Fill in the fields below, then change draft to false.
#   4. Save, preview, commit and deploy.
#
# The `amount` is added to the home-page "secured in grants & funding" total.
# The funder (the organisation) is credited under "Funders & grants" in the
# supporters list. Only ever enter a figure you can back up (report, receipt).
# ────────────────────────────────────────────────────────────────
org: australian-government          # organisation id from src/content/organisations.ts
amount: 0                           # dollars only, no $ or commas, e.g. 2500
# role: funder                      # optional — overrides the org's default role
                                    # (funder | partner | patron | supporter | school)
source: 'Where this figure comes from, e.g. Treasurer''s Report DD/MM/YYYY.'
draft: true                         # set to false to include it
---
